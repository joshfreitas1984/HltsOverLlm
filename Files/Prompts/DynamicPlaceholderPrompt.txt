**Placeholders:**
- Keep placeholders like `{0}`, `{1}`, `{2}`, `{3}`, `{name_1}`, and `{name_2}` unchanged.
- Use `{name_1}` as a surname contextually. 
- Use `{name_2}` as a first name contextually.
- Treat `{0}`, `{1}`, `{2}`, `{3}` as objects based on context.